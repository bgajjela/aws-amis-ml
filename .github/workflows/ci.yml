name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    name: Lint, CIS check, CVE scan
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Install trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sudo sh -s -- -b /usr/local/bin v0.70.0

      - name: Install packer
        run: |
          curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" \
            | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update && sudo apt-get install -y packer

      - name: ShellCheck
        run: |
          shellcheck -S warning harden.sh \
            scripts/ami-finalize.sh \
            scripts/build-pro-envs.sh \
            scripts/spark-java.sh \
            docker-sim/run-provision.sh

      - name: CIS compliance check
        run: bash tests/cis-check.sh

      - name: Trivy — CVE + secret + misconfiguration scan
        run: |
          trivy fs \
            --scanners vuln,secret,misconfig \
            --severity HIGH,CRITICAL \
            --exit-code 1 \
            .

      - name: Packer validate
        run: |
          packer init .
          packer validate \
            -var subnet_id=subnet-placeholder \
            -var security_group_id=sg-placeholder \
            .

  # Optional: build AMIs on tagged releases when AWS credentials are configured.
  # Uncomment and add AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_DEFAULT_REGION
  # as repository secrets to enable.
  #
  # build:
  #   name: Build AMIs
  #   needs: lint-and-test
  #   runs-on: ubuntu-22.04
  #   if: startsWith(github.ref, 'refs/tags/v')
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
  #     - name: Build base AMI
  #       run: packer build -var spot_price=auto -only=cpu-ds-ml-base .
  #     - name: Build pro AMI
  #       run: packer build -var spot_price=auto -only=cpu-ds-ml-pro .
